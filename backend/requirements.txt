fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.11.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.1

# AWS — DynamoDB(커밋 저장 + 캐시) + Bedrock(LLM)
boto3>=1.35.0
aioboto3>=12.0.0          # DynamoDB 비동기 접근 (FastAPI async 호환)
langchain-aws>=0.2.0      # ChatBedrock

# Anthropic SDK — blame/traceability 기능용
anthropic>=0.34.0
langchain-anthropic>=0.3.0

# LangGraph / LangChain
langgraph>=0.2.0
langchain-core>=0.3.0
