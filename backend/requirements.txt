fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.11.0
pydantic-settings>=2.0.0   # FastAPI 정석 설정 관리 (BaseSettings)
python-dotenv>=1.0.1

# AWS — Bedrock(LLM) + DynamoDB(캐시)
boto3>=1.35.0
langchain-aws>=0.2.0          # ChatBedrock, BedrockEmbeddings

# Anthropic SDK — blame/traceability 기능용 (Bedrock 미사용 기능)
anthropic>=0.34.0
langchain-anthropic>=0.3.0

# LangGraph / LangChain
langgraph>=0.2.0
langchain-core>=0.3.0

# RDS PostgreSQL
sqlalchemy[asyncio]>=2.0.0   # 비동기 ORM
asyncpg>=0.29.0               # 앱용 비동기 드라이버
psycopg2-binary>=2.9.0        # alembic 마이그레이션용 동기 드라이버
alembic>=1.13.0               # DB 스키마 관리
